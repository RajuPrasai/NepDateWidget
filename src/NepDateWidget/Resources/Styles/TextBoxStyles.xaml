<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ══ Themed TextBox context menu ════════════════════════════════════════
         Applied to all TextBox styles below so Cut/Copy/Paste matches the widget theme. -->
    <ContextMenu x:Key="ThemedTextBoxContextMenu">
        <ContextMenu.Style>
            <Style TargetType="ContextMenu">
                <Setter Property="Background"      Value="{DynamicResource WidgetBackgroundBrush}" />
                <Setter Property="Foreground"      Value="{DynamicResource WidgetForegroundBrush}" />
                <Setter Property="BorderBrush"     Value="{DynamicResource WidgetBorderBrush}" />
                <Setter Property="BorderThickness" Value="0.7" />
                <Setter Property="HasDropShadow"   Value="True" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ContextMenu">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"
                                    Padding="0,4"
                                    Margin="4"
                                    SnapsToDevicePixels="True"
                                    TextOptions.TextFormattingMode="Display"
                                    TextOptions.TextRenderingMode="ClearType"
                                    TextOptions.TextHintingMode="Fixed"
                                    RenderOptions.ClearTypeHint="Enabled">
                                <ItemsPresenter SnapsToDevicePixels="True" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ContextMenu.Style>
        <ContextMenu.Resources>
            <Style TargetType="MenuItem">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource WidgetForegroundBrush}" />
                <Setter Property="OverridesDefaultStyle" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="MenuItem">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="4"
                                    Margin="4,1"
                                    Padding="10,6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      ContentSource="Header"
                                                      VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1"
                                               Text="{TemplateBinding InputGestureText}"
                                               Foreground="{DynamicResource WidgetMutedForegroundBrush}"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               Margin="16,0,0,0" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background"
                                            Value="{DynamicResource WidgetAccentBrushLight}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ContextMenu.Resources>
        <MenuItem Command="ApplicationCommands.Cut" Header="Cut" InputGestureText="Ctrl+X" />
        <MenuItem Command="ApplicationCommands.Copy" Header="Copy" InputGestureText="Ctrl+C" />
        <MenuItem Command="ApplicationCommands.Paste" Header="Paste" InputGestureText="Ctrl+V" />
    </ContextMenu>

    <!-- Single-line placeholder template.
         Padding on the Border positions both PART_ContentHost and Placeholder.
         WPF TextBoxView adds ~2 px internal left offset for the caret;
         the placeholder uses Margin="2,0,0,0" to match that offset. -->
    <ControlTemplate x:Key="PlaceholderTextBoxTemplate" TargetType="TextBox">
        <Border Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="6"
                Padding="{TemplateBinding Padding}">
            <Grid>
                <ScrollViewer x:Name="PART_ContentHost"
                              VerticalAlignment="Center"
                              VerticalScrollBarVisibility="Hidden"
                              HorizontalScrollBarVisibility="Hidden"
                              Padding="0" />
                <TextBlock x:Name="Placeholder"
                           Text="{TemplateBinding Tag}"
                           Foreground="{DynamicResource WidgetMutedForegroundBrush}"
                           FontSize="{TemplateBinding FontSize}"
                           Margin="6,0,0,0"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False"
                           Visibility="Collapsed" />
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="Text" Value="">
                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Multiline placeholder template -->
    <ControlTemplate x:Key="MultilinePlaceholderTemplate" TargetType="TextBox">
        <Border Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="6"
                Padding="{TemplateBinding Padding}">
            <Grid>
                <ScrollViewer x:Name="PART_ContentHost"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Padding="0" />
                <TextBlock x:Name="Placeholder"
                           Text="{TemplateBinding Tag}"
                           Foreground="{DynamicResource WidgetMutedForegroundBrush}"
                           FontSize="{TemplateBinding FontSize}"
                           Margin="6,0,0,0"
                           VerticalAlignment="Top"
                           IsHitTestVisible="False"
                           Visibility="Collapsed"
                           TextWrapping="Wrap" />
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="Text" Value="">
                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Multiline result template (no placeholder, scrolling only) -->
    <ControlTemplate x:Key="MultilineResultTemplate" TargetType="TextBox">
        <Border Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="6"
                Padding="{TemplateBinding Padding}">
            <ScrollViewer x:Name="PART_ContentHost"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="0" />
        </Border>
    </ControlTemplate>

    <!-- Single-line editable input -->
    <Style x:Key="InputBoxStyle" TargetType="TextBox">
        <Setter Property="Background"      Value="{DynamicResource WidgetBackgroundBrush}" />
        <Setter Property="Foreground"      Value="{DynamicResource WidgetForegroundBrush}" />
        <Setter Property="BorderBrush"     Value="{DynamicResource WidgetInputBorderBrush}" />
        <Setter Property="BorderThickness" Value="0.7" />
        <Setter Property="Height"          Value="36" />
        <Setter Property="FontSize"        Value="13" />
        <Setter Property="Padding"         Value="3,0" />
        <Setter Property="CaretBrush"      Value="{DynamicResource WidgetForegroundBrush}" />
        <Setter Property="SelectionBrush"  Value="{DynamicResource WidgetAccentBrush}" />
        <Setter Property="ContextMenu"     Value="{StaticResource ThemedTextBoxContextMenu}" />
        <Setter Property="Template"        Value="{StaticResource PlaceholderTextBoxTemplate}" />
    </Style>

    <!-- Single-line read-only output -->
    <Style x:Key="OutputBoxStyle" TargetType="TextBox">
        <Setter Property="Background"      Value="{DynamicResource WidgetInputBrush}" />
        <Setter Property="Foreground"      Value="{DynamicResource WidgetSuccessBrush}" />
        <Setter Property="BorderBrush"     Value="{DynamicResource WidgetInputBorderBrush}" />
        <Setter Property="BorderThickness" Value="0.7" />
        <Setter Property="Height"          Value="36" />
        <Setter Property="FontSize"        Value="13" />
        <Setter Property="FontWeight"      Value="SemiBold" />
        <Setter Property="Padding"         Value="3,0" />
        <Setter Property="IsReadOnly"      Value="True" />
        <Setter Property="CaretBrush"      Value="{DynamicResource WidgetSuccessBrush}" />
        <Setter Property="SelectionBrush"  Value="{DynamicResource WidgetAccentBrush}" />
        <Setter Property="ContextMenu"     Value="{StaticResource ThemedTextBoxContextMenu}" />
        <Setter Property="Template"        Value="{StaticResource PlaceholderTextBoxTemplate}" />
    </Style>

    <!-- Multiline editable input -->
    <Style x:Key="MultilineInputStyle" TargetType="TextBox">
        <Setter Property="Background"      Value="{DynamicResource WidgetBackgroundBrush}" />
        <Setter Property="Foreground"      Value="{DynamicResource WidgetForegroundBrush}" />
        <Setter Property="BorderBrush"     Value="{DynamicResource WidgetInputBorderBrush}" />
        <Setter Property="BorderThickness" Value="0.7" />
        <Setter Property="FontSize"        Value="13" />
        <Setter Property="Padding"         Value="3,3" />
        <Setter Property="CaretBrush"      Value="{DynamicResource WidgetForegroundBrush}" />
        <Setter Property="SelectionBrush"  Value="{DynamicResource WidgetAccentBrush}" />
        <Setter Property="TextWrapping"    Value="Wrap" />
        <Setter Property="AcceptsReturn"   Value="True" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ContextMenu"     Value="{StaticResource ThemedTextBoxContextMenu}" />
        <Setter Property="Template"        Value="{StaticResource MultilinePlaceholderTemplate}" />
    </Style>

    <!-- Multiline read-only output -->
    <Style x:Key="MultilineOutputStyle" TargetType="TextBox">
        <Setter Property="Background"      Value="{DynamicResource WidgetInputBrush}" />
        <Setter Property="Foreground"      Value="{DynamicResource WidgetForegroundBrush}" />
        <Setter Property="BorderBrush"     Value="{DynamicResource WidgetInputBorderBrush}" />
        <Setter Property="BorderThickness" Value="0.7" />
        <Setter Property="FontSize"        Value="13" />
        <Setter Property="Padding"         Value="3,3" />
        <Setter Property="IsReadOnly"      Value="True" />
        <Setter Property="CaretBrush"      Value="{DynamicResource WidgetForegroundBrush}" />
        <Setter Property="SelectionBrush"  Value="{DynamicResource WidgetAccentBrush}" />
        <Setter Property="TextWrapping"    Value="Wrap" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="ContextMenu"     Value="{StaticResource ThemedTextBoxContextMenu}" />
        <Setter Property="Template"        Value="{StaticResource MultilineResultTemplate}" />
    </Style>

    <!-- Multiline monospace result output -->
    <Style x:Key="ResultBoxStyle" TargetType="TextBox" BasedOn="{StaticResource MultilineOutputStyle}">
        <Setter Property="FontFamily" Value="Consolas, Courier New, monospace" />
    </Style>

    <!-- ══ Themed ComboBox (shared across views) ═════════════════════════════ -->
    <Style x:Key="ThemedComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Height"          Value="32" />
        <Setter Property="Background"      Value="{DynamicResource WidgetBackgroundBrush}" />
        <Setter Property="Foreground"      Value="{DynamicResource WidgetForegroundBrush}" />
        <Setter Property="BorderBrush"     Value="{DynamicResource WidgetInputBorderBrush}" />
        <Setter Property="BorderThickness" Value="0.7" />
        <Setter Property="FontSize"        Value="13" />
        <Setter Property="Padding"         Value="6,0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border x:Name="MainBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" />
                        <ToggleButton Focusable="False" Background="Transparent" BorderThickness="0"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                                   RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                            <ToggleButton.Style>
                                <Style TargetType="ToggleButton">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent" />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ToggleButton.Style>
                        </ToggleButton>
                        <ContentPresenter Margin="8,0,28,0"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                          IsHitTestVisible="False" />
                        <TextBlock Text="▾" FontSize="11"
                                   Foreground="{TemplateBinding Foreground}"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Right"
                                   Margin="0,0,8,0" IsHitTestVisible="False" />
                        <Popup Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="False" Focusable="False"
                               PopupAnimation="None">
                            <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Background="{DynamicResource WidgetInputBrush}"
                                    BorderBrush="{DynamicResource WidgetInputBorderBrush}"
                                    BorderThickness="0.7" CornerRadius="4" Margin="0,2,0,0">
                                <Border.Resources>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Background"  Value="{DynamicResource WidgetInputBrush}" />
                                        <Setter Property="Foreground"  Value="{DynamicResource WidgetForegroundBrush}" />
                                        <Setter Property="Padding"     Value="8,5" />
                                        <Setter Property="BorderThickness" Value="0" />
                                        <Style.Triggers>
                                            <Trigger Property="IsHighlighted" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource WidgetHoverBrush}" />
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource WidgetHoverBrush}" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Resources>
                                <ScrollViewer SnapsToDevicePixels="True">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="Background"
                                    Value="{DynamicResource WidgetAccentBrushLight}" />
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="MainBorder" Property="Background"
                                    Value="{DynamicResource WidgetAccentBrushLight}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
